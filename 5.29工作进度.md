# 5.29 工作进度

> 更新日期：2026-05-29

---

## ✅ 已完成

### 1. 代码整理与提交
- 提交未提交变更（admin后台、支付API、DB迁移）
- 推送至 GitHub：`https://github.com/barronli0523/prompt-genius`

### 2. 阿里云 OSS 配置
- 创建 Bucket：`prompt-genius`（美东节点）
- 清理误传的 node_modules 和源码文件
- 上传 `public/hero-banner.png` 至 `assets/hero-banner.png`
- 设置 Bucket 为公开读权限
- 公共 URL：`https://prompt-genius.oss-us-east-1.aliyuncs.com/assets/hero-banner.png`
- 已打包 OSS 压缩包至 `public-oss-assets.tar.gz`

### 3. 数据库配置
- 替换 `SUPABASE_SERVICE_ROLE_KEY` 为真实值（Supabase Dashboard 获取）
- 在 Supabase SQL Editor 执行 `sql/migration.sql`
- 创建表：`subscriptions`、`orders`
- 新增索引和触发器

### 4. 后台验证
- Admin API (`/api/admin/stats`) 返回成功
- 数据库表结构正常
- TypeScript 类型检查通过
- `npm run build` 构建成功

### 5. ESLint 配置修复
- 更新为 `typescript-eslint` + `@next/eslint-plugin-next`
- 构建无 ESLint 错误

---

## ⏳ 待办事项

### Phase 1: ECS 部署（等待 ECS 信息）
- [ ] 获取 ECS 公网 IP
- [ ] 获取 SSH 登录凭证
- [ ] 安装 Node.js 20、PM2、Nginx、Git
- [ ] 克隆代码到服务器
- [ ] 配置 `.env` 环境变量
- [ ] 构建并启动 Next.js（PM2 管理进程）
- [ ] 配置 Nginx 反向代理
- [ ] 安装 Certbot 配置 HTTPS

### Phase 2: 域名配置（等待域名信息）
- [ ] 获取已购买域名
- [ ] 添加 DNS A 记录（@ → ECS IP）
- [ ] 添加 DNS A 记录（www → ECS IP）
- [ ] 确认 ICP 备案状态
- [ ] Clerk Dashboard 添加 Allowed redirect URLs

### Phase 3: 支付接口对接（后续）
- [ ] 获取微信/支付宝商户参数
- [ ] 实现真实支付流程（`/api/payment/create`）
- [ ] 微信验签回调（`/api/payment/wechat-notify`）
- [ ] 支付宝验签回调（`/api/payment/alipay-notify`）

### Phase 4: 上线后
- [ ] 替换 Clerk test key 为 production key
- [ ] 监控服务器日志和性能
- [ ] 配置 CDN（可选）

---

## 📋 关键信息汇总

| 项目 | 值 |
|------|------|
| GitHub 仓库 | `https://github.com/barronli0523/prompt-genius` |
| Supabase URL | `https://wzgtllbonthwrvbwvggw.supabase.co` |
| OSS Bucket | `prompt-genius` (oss-us-east-1) |
| OSS 图片 URL | `https://prompt-genius.oss-us-east-1.aliyuncs.com/assets/hero-banner.png` |
| 当前状态 | 本地开发正常，待 ECS 部署上线 |

---

## 🔑 环境变量清单（.env.local）

| 变量 | 状态 | 备注 |
|------|------|------|
| `OPENAI_API_KEY` | ⚠️ 占位符 | 需替换为阿里云百炼 Key |
| `NEXT_PUBLIC_SUPABASE_URL` | ✅ 已配置 | Supabase 项目地址 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✅ 已配置 | 前端 anon key |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ 已配置 | 后端 service_role key |
| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` | ⚠️ test key | 上线后需切换 production |
| `CLERK_SECRET_KEY` | ⚠️ test key | 上线后需切换 production |
| `STRIPE_*` | ⚠️ 占位符 | 支付接口暂未启用 |
